# أفضل ممارسات اختبار الهوكات وأداء WolFox على iOS الحديثة

## الهدف وحدود الاختبار

يجب أن يثبت الاختبار أمرين منفصلين: **سلامة منطق الهوك**، أي أن السلوك الأصلي يعود كما كان عند إيقاف الميزة أو عند أي خطأ؛ و**سلامة الأداء**، أي أن الهوك لا يضيف زمناً أو استهلاكاً أو أعطالاً غير مقبولة في التطبيقات المصرح باختبارها. لا يكفي نجاح التثبيت أو ظهور الواجهة؛ فالقياس يجب أن يكون على جهاز فعلي وبسيناريوهات استخدام محددة.

> توصي Apple بجمع قياسات قابلة للتكرار ثم وضع baseline وحدّ تقلب، وتشغيل قياسات الأداء في إعداد Release مع تعطيل Debug executable وcode coverage وخيارات sanitization أثناء القياس. [1]

المسار الحالي من WolFox ينتج **arm64 فقط**. لا تفسّر نجاح تثبيته على أنه تحقق تشغيلي من عمليات `arm64e`. إذا كان التطبيق المستهدف مبنياً لـ `arm64e`، فيلزم لاحقاً إنشاء شريحة `arm64e` حقيقية والتحقق من رأس Mach-O الخاص بها قبل اختبار الحقن في تلك العملية. تشير Theos إلى أن arm64e تختلف عن arm64 بسبب pointer authentication، وأنها مطلوبة عند الحقن في ثنائيات نظام مبنية بها. [4]

| طبقة الاختبار | ما الذي يثبت؟ | بيئة التنفيذ |
|---|---|---|
| اختبارات منطقية | التحقق من الإحداثيات، الحالة، التخزين، والترخيص | Linux أو XCTest للوظائف المنعزلة |
| اختبارات التكامل | تركيب الهوك، حراسة إعادة الدخول، واستدعاء الأصل | تطبيق اختبار تملكه على جهاز iOS |
| اختبارات واجهة | الظهور والإخفاء والتفاعل والحفظ والاسترجاع | XCUIAutomation أو خطوات يدوية موثقة |
| اختبارات أداء | زمن callback، CPU، الذاكرة، الطاقة، وhangs | جهاز iOS فعلي + Instruments |
| اختبارات استقرار ممتد | تبديل الحالة والخلفية والاستئناف والتكرار | جهاز فعلي وجلسات زمنية مضبوطة |

## 1. اعمل بمصفوفة اختبار ثابتة

ابدأ بتطبيق اختبار تملكه أو بيئة مخبرية مصرح بها، ولا تختبر على حسابات إنتاج أو تطبيقات لا تملك تفويضاً صريحاً لاختبارها. سجّل إصدار WolFox، إصدار iOS الدقيق، طراز الجهاز، نوع الحزمة Rootful أو Rootless، وحالة التفعيل لكل تشغيل. لا تخلط عدة تعديلات في التجربة الأولى؛ اختبر WolFox وحدها ثم أضف التعديلات الأخرى تدريجياً لتحديد مصدر التعارض.

| البعد | الحد الأدنى المقترح |
|---|---|
| الإصدار | أحدث إصدار ثابت من iOS 18 المتاح لديك، وإصدار سابق للمقارنة إن أمكن |
| الدورة الحياتية | تشغيل بارد، تشغيل دافئ، انتقال للخلفية، استئناف، إغلاق قسري ثم تشغيل |
| حالة WolFox | غير مفعلة، مفعلة بلا ميزة، تزييف مفعّل، تزييف متوقف واستعادة الأصل |
| نمط الموقع | موقع حقيقي، إحداثيات صحيحة، إحداثيات حدية، وإحداثيات غير صالحة |
| الضغط | تبديل الحالة سريعاً، إنشاء عدة managers/delegates، واستخدام الواجهة أثناء وصول callbacks |
| التراجع | تعطيل الميزة، حذف الحزمة، إعادة فتح التطبيق المستهدف، والتأكد من السلوك الأصلي |

## 2. اختبر عقد كل Hook قبل الأداء

يُعامل كل هوك كعقد واضح: ما هي نقطة الدخول، وما الشرط الذي يفعّله، وما القيمة البديلة، ومتى يُستدعى التنفيذ الأصلي. أضف سجلاً مختصراً للنتيجة فقط، ولا تسجّل أسرار الترخيص أو UUID الكامل أو مواقع حساسة في سجل دائم.

| اختبار العقد | معيار النجاح |
|---|---|
| الهوك غير مفعّل | يُستدعى IMP/الدالة الأصلية وتبقى البيانات الأصلية كما هي |
| الترخيص غير صالح أو منتهي | لا يظهر سلوك معدل ولا واجهة مميزات محمية |
| قيمة إدخال غير صالحة | fallback آمن إلى الأصل، من دون exception أو callback ناقص |
| إعادة الدخول | الهوك لا يعيد استدعاء نفسه ولا يكرر تغليف delegate |
| delegate | يصل callback مرة واحدة وبنفس ترتيب البيانات المتوقع |
| تبديل الحالة | التطبيق الفوري أو الإيقاف الفوري لا يترك حالة نصف مفعلة |
| ذاكرة الكائنات | لا يحتفظ proxy بمرجع قوي يمنع تحرير delegate أو manager |

القاعدة العملية هي أن يكون تركيب الهوك **idempotent**: نفّذ التركيب مرة واحدة فقط، واحمِ المسار من إعادة الدخول، وتأكد من وجود الكلاس والـ selector قبل استخدامهما. عند فشل أي شرط، اخرج مباشرة إلى التنفيذ الأصلي. انقل تحديثات UIKit دائماً إلى main thread، ولا تنفّذ I/O أو شبكة أو إنشاء واجهة داخل callback عالية التردد.

## 3. استخدم سجلات قياسية ونقاط قياس قصيرة

قسّم مسار القياس إلى نقاط يمكن مقارنتها، مثل: دخول hook، قرار التفعيل، إنشاء القيمة البديلة، إرسال delegate، وتحديث الواجهة. استخدم `os_log` بفئات واضحة، واستخدم `os_signpost` أو مكافئه فقط حول فترات قصيرة ذات معنى؛ أمثلة مناسبة هي زمن معالجة callback أو زمن تطبيق تغيير الحالة.

لا تضع signpost لكل قراءة موقع في وضع إنتاج إذا كان التردد عالياً. استخدم sampling أو فعّل القياس في بناء تشخيصي محدود. تحذر Apple من أن قياس الأداء يحتاج إلى ظروف قابلة للتكرار؛ ولذلك اجعل سجلات التشخيص قابلة للتشغيل والإيقاف ولا تجعلها جزءاً ثابتاً من مسار الأداء. [1]

## 4. قس الأداء على جهاز فعلي

القياس في المحاكي مفيد للمنطق، لكنه لا يكفي لاختبار زمن الاستجابة والطاقة والذاكرة الخاصة بعملية iOS الفعلية. توصي Apple بالقياس على الجهاز عندما تريد دقة أعلى، وباختيار قالب Instruments وفق العرض المرصود. [2]

| العرض | الأداة | السؤال الذي تجيب عنه |
|---|---|---|
| بطء أو تجمد | Time Profiler | أين تُستهلك CPU، وهل يظهر عمل متكرر في hook أو main thread؟ |
| نمو الذاكرة | Allocations وLeaks | هل تبقى proxies أو blocks أو صور أو delegates في الذاكرة بعد الإغلاق؟ |
| طاقة مرتفعة | Energy Log | هل يرفع التحديث الدوري أو الموقع أو الـ timers استهلاك الطاقة؟ |
| I/O غير مبرر | File Activity | هل يتم حفظ إعدادات أو سجلات بوتيرة غير ضرورية؟ |
| بطء شبكة أو تفعيل | Network template | هل توجد طلبات تفعيل متكررة أو متزامنة على main thread؟ |

قارن كل تجربة بحالة ضابطة: **WolFox محملة لكن الميزة مطفأة**، ثم ميزة واحدة مفعلة. سجّل median وp95 لزمن callback، متوسط CPU، أعلى ذاكرة، عدد callbacks، وعدد crashes أو hangs. لا تعتمد رقماً عاماً ثابتاً؛ عيّن baseline على الجهاز المستهدف ثم فشل الاختبار إذا تعدى القياس tolerance الموثق. يتيح XCTest قياس blocks ووضع baseline وMax STDDEV لهذا الغرض. [1]

## 5. اجعل اختبار الضغط محدداً وقابلاً للإعادة

نفّذ جلسة ضغط قصيرة أولاً، ثم جلسة استقرار أطول. لا تغيّر أكثر من عامل واحد في الجولة. مثال عملي: بدّل حالة الميزة 50 مرة، أرسل التطبيق للخلفية واستأنفه 20 مرة، وأنشئ أو حرر عدة managers/delegates، ثم راقب أن عدد callbacks لا يتضاعف وأن الذاكرة تعود إلى نطاقها الأساسي بعد انتهاء السيناريو.

| السيناريو | ما الذي يراقب؟ | علامة الإخفاق |
|---|---|---|
| تفعيل/إيقاف متكرر | ذرية الحالة وidempotence | callback مكرر أو حالة محفوظة خاطئة |
| خلفية ثم استئناف | timers والمراجع الضعيفة | crash أو memory growth أو update متأخر |
| إعادة فتح التطبيق | تخزين الإعدادات والترخيص | ظهور مميزات بلا ترخيص صالح أو فقدان الإعدادات المصرح بها |
| عدة managers | جدول proxies وحماية التزامن | delegate مفقود أو وصول مزدوج |
| اختيار صورة/كاميرا | مسار الوسائط والذاكرة | تسرب image object أو حجب main thread |

## 6. افصل اختبارات الصحة عن اختبارات الأداء

شغّل AddressSanitizer وUndefinedBehaviorSanitizer واختبارات verbose في جولة صحة منفصلة، ثم أوقفها عند أخذ أرقام الأداء. Apple توصي بتعطيل sanitization وcode coverage أثناء performance tests لأنهما يغيران القياس. [1] اجعل اختبار الصحة يثبت عدم وجود crash أو استخدام غير صحيح للذاكرة، واجعل اختبار الأداء يثبت عدم وجود regression مقارنة بالنسخة الضابطة.

يمكن استخدام XCTest للاختبارات المنطقية وUI tests وperformance tests ضمن workflow واحد؛ كما تدعم XCTest assertions وexpected failures وواجهات UI testing. [3] حتى إن بقي جزء من WolFox Objective-C++ داخل dylib، من المفيد استخراج منطق التحقق والحالة والحسابات إلى وحدات يمكن اختبارها دون تحميل الهوك نفسه.

## 7. مراقبة ما بعد الإصدار

إذا كان لديك تطبيق مرافق تملكه، يمكن أن يقدم MetricKit رؤية مجمعة عن الإقلاع، الاستجابة، الذاكرة، CPU، الطاقة، crashes وhangs على الأجهزة الحقيقية. لا تستخدمه لجمع بيانات من التطبيقات المستهدفة؛ اجعله محصوراً في التطبيق المرافق أو بيئة الاختبار التي تملكها. توضح Apple أن تقارير MetricKit تصل وفق جدول يحدده النظام، وأنها تشمل بيانات تشخيصية ومقاييس أداء مجمعة. [2] [5]

قبل كل إصدار، احتفظ بملف نتيجة يتضمن نسخة الحزمة، SHA-256، طراز الجهاز، iOS build، سيناريو الاختبار، baseline، القيم المقاسة، وأثر التراجع. اعتمد الإصدار فقط عندما يجتاز اختبار الوظيفة، واختبار الضغط، ومقارنة الأداء، وخطة التراجع.

## بوابة إصدار مختصرة

| شرط الإصدار | قرار القبول |
|---|---|
| جميع اختبارات العقد ناجحة | مطلوب |
| لا crash أو hang جديد في سيناريوهات الدورة الحياتية | مطلوب |
| p95 وزمن الاستجابة ضمن tolerance الموثق | مطلوب |
| الذاكرة تعود إلى baseline بعد الجلسة | مطلوب |
| فلتر Bundle IDs مقيد بالتطبيقات المصرح بها | مطلوب |
| يمكن تعطيل الميزة أو إزالة الحزمة واستعادة السلوك الأصلي | مطلوب |
| arm64e | لا تُعلن إلا بعد إنتاج واختبار شريحة arm64e حقيقية مستقلة |

## المراجع

[1] [Apple Developer — Writing and running performance tests](https://developer.apple.com/documentation/xcode/writing-and-running-performance-tests)

[2] [Apple Developer — Improving your app’s performance](https://developer.apple.com/documentation/xcode/improving-your-app-s-performance)

[3] [Apple Developer — XCTest](https://developer.apple.com/documentation/xctest)

[4] [Theos — arm64e Deployment](https://theos.dev/docs/arm64e-deployment)

[5] [Apple Developer — Track performance by app state using MetricKit](https://developer.apple.com/documentation/metrickit/track-performance-by-app-state-using-metrickit)
