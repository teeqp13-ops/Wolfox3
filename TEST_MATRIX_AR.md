# مصفوفة اختبار Wolfox v1.6.1

## توافق النظام والبناء

- iOS 15.8: اختبار الحد الأدنى على Rootful أو Rootless وفق نوع الجيلبريك.
- iOS 16.x و17.x و18.x: اختبار Rootless وإعادة الحقن بعد Respring.
- iOS 19.x حتى 26.5: لا يُعلن التوافق إلا بعد اختبار كل إصدار فعلي متاح.
- فحص شريحة dylib الناتجة والتأكد من أنها `arm64` واحدة بلا ادعاء `arm64e`.
- التأكد من أن Minimum OS في الملف الناتج هو iOS 15.8.
- التأكد من وجود توقيع Mach-O بعد تشغيل `ldid -S`، وأن ملكية ملفات DEB هي `root:root`.
- عدم استخدام ملف Rootful على جيلبريك Rootless أو العكس.
- تحقق أن Rootful تحمل `iphoneos-arm` وRootless تحمل `iphoneos-arm64`.
- تحقق أن `dpkg-deb -f PACKAGE.deb Depends` يعرض `firmware (>= 15.8), mobilesubstrate`.

## الترخيص

- كود صالح: تظهر الواجهة فقط بعد نجاح التحقق.
- كود منتهي/محظور: تتوقف كل الهوكات وتظهر شاشة التفعيل.
- مشروع متوقف: تتوقف الهوكات مع بقاء الكود محفوظاً.
- تحديث إجباري: يظهر زر التحديث الأزرق ولا يمكن تجاوز شاشة التفعيل.
- انقطاع شبكة: يستخدم تحققاً مخزناً لا يتجاوز 24 ساعة ولا يتجاوز تاريخ الانتهاء.
- Access token غير صالح: يبقى الكود معروضاً لإعادة التفعيل.

## أزرار الصوت والنافذة

- ثلاث ضغطات سريعة تُظهر الأداة.
- زر الإخفاء يعيد التحكم للتطبيق المضيف.
- ثلاث ضغطات جديدة بعد الإخفاء تُظهر الأداة مرة أخرى.
- إغلاق التطبيق وفتحه بعد الإخفاء لا يعرض Wolfox تلقائياً.
- بعد إعادة فتح التطبيق، ثلاث ضغطات صوت تعرضها مباشرة.
- يجب ظهور السجل `hidden_volume_listener_active` بعد الإخفاء، ثم سجلات
  `volume_request_progress=1/3` حتى `3/3` عند الضغط.
- اختبر الأزرار والصوت في المنتصف وعند الحد الأعلى/الأدنى، مع تشغيل صوت داخل
  التطبيق المضيف ومن دونه.
- فشل Heartbeat أثناء الإخفاء لا يفتح شاشة التفعيل تلقائياً؛ تظهر فقط بعد أمر الصوت.
- الإخفاء يزيل الواجهة وأيقونة الكاميرا العائمة بالكامل.
- الضغطة الأصلية للصوت لا تُمنع.
- لا يحدث فتح/إغلاق مزدوج عند عمل إشعار النظام وUIApplication وKVO معاً.

## GPS

- تشغيل وإيقاف الموقع الوهمي.
- زر التوسيع ينقل الخريطة نفسها إلى ملء الشاشة ويحافظ على الدبابيس والنمط.
- زر الإغلاق يعيد الخريطة وشريط البحث إلى البطاقة من دون فقد الحالة.
- البحث بإحداثيات إنجليزية: `24.7136, 46.6753`.
- البحث بإحداثيات عربية: `٢٤٫٧١٣٦، ٤٦٫٦٧٥٣`.
- البحث باسم مدينة أو مكان أو عنوان، مع رسالة واضحة عند عدم وجود نتيجة.
- الضغط المطول داخل الوضع الموسع يحدّث الدبوس وحقول Latitude/Longitude.
- إخفاء Wolfox أو تبديل التبويب يغلق وضع الخريطة الموسعة بأمان.
- تغيير السرعة وبدء/إيقاف المسار.
- إيقاف الموقع يوقف مؤقت المسار.
- البحث الجغرافي لا يتأثر بهوك coordinate الداخلي.

## البلوتوث

- مسح Wolfox نفسه يعرض الأجهزة الحقيقية.
- حفظ ملف وتفعيله ثم بدء مسح داخل التطبيق المستهدف.
- النتيجة تعرض الاسم/UUID/RSSI للملف النشط.
- تغيير الملف يعيد المسح بهوية الملف الجديد.

## الكاميرا

- اختيار صورة من داخل Wolfox وحفظها.
- طلب الكاميرا من تطبيق يستخدم UIImagePickerController.
- وصول الصورة إلى delegate مرة واحدة.
- حذف الصورة يعيد الكاميرا الحقيقية.

## الواجهة

- خلفية داكنة ثابتة.
- شاشة الكود وأزرارها باللون الأزرق.
- الاسم الظاهر `Wolfox` فقط.
- نافذة معلومات الاشتراك تستخدم الألوان نفسها وتصبح قابلة للتمرير على الشاشات الصغيرة.
- حالة الرأس تعرض رمزاً ونص «نشط/متوقف» وتُحدّث مع GPS والبلوتوث والكاميرا.
- المفضلة الفارغة تعرض `star.slash` ونصاً إرشادياً.
- إيقاف التزييف والحذف يطلبان تأكيداً أحمر واضحاً.
- جميع عناصر اللمس الرئيسية 44×44 نقطة على الأقل.
- عند تفعيل Reduce Motion تختفي الحركات المتكررة وتصبح الانتقالات فورية.
