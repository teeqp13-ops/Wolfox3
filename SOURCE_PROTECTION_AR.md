# حالة السورس والحماية

السورس في هذه الحزمة واضح وغير مشفر، ولا توجد سكربتات AES أو فك تشفير. يطبق
مسار البناء تقوية ثنائية محدودة فقط عبر `-fvisibility=hidden` و`-fno-common`
و`dead_strip/-x` عندما يقبلها linker، مع إعادة ربط آمنة بالخيارات الأساسية إذا
رفضها. لا تُغيّر أسماء كلاسات Objective-C أو selectors اللازمة للهوكات.

يمكن تعطيل التقوية للتشخيص فقط:

```bash
WOLFOX_HARDENING=0 ./build_v1_deb.sh
```

توقيع dylib بواسطة `ldid` يظل إلزامياً للبناء النهائي، ولا تُحفظ أسرار توقيع
الخادم داخل السورس؛ `WF_PROJECT_KEY` معرف مشروع عام فقط.
