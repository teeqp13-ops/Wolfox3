# جاهزية الإصدار

## إصلاحات هذه النسخة

- تصحيح حقل `Depends` ليصبح حقلاً مستقلاً قابلاً للقراءة بواسطة `dpkg-deb`.
- ضبط Rootful على `iphoneos-arm` وRootless على `iphoneos-arm64`.
- إضافة تحقق بعد التغليف للمعمارية والاعتمادات والمسار والملكية.
- إزالة هوك تعديل JSON ومفاتيح تجاوز قيود الجهاز.
- استبدال قائمة التطبيقات الخارجية بمعرف اختبار توضيحي واحد.
- إضافة nullability كاملة إلى `WolFoxProStore.h` مع تعليم الحقول الاختيارية.
- إزالة رؤوس UIUtilities المؤقتة والوثائق القديمة الخاصة بـSDK 26.5.
- إزالة سكربتات تشفير السورس؛ الحزمة واضحة بالكامل.

## ما يثبته Linux

تثبت الاختبارات الأنماط المصدرية ومحاكاة منطق الموقع والمعرفات وسلامة سكربتات
البناء. لا تثبت تحميل MobileSubstrate أو استجابة أزرار الصوت أو عمل UIKit وCore
Location على جهاز حقيقي.

## شرط التوزيع

قبل أي توزيع: استبدل Bundle ID التوضيحي بتطبيق اختبار مملوك أو مصرح به، شغّل
`run_all_linux_tests.sh`، ابنِ الحزمتين، ثم اختبرهما على جهاز iOS فعلي مناسب.
