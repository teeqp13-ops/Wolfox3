# سياسة حماية WolFox

## البيانات السرية

لا يحتوي المستودع على مفتاح توقيع خادم أو كلمة مرور قاعدة بيانات أو رمز نشر.
`WF_PROJECT_KEY` معرّف مشروع عام فقط، أما سر توقيع التراخيص فيبقى على الخادم.
رموز الاشتراك المستلمة أثناء التشغيل تحفظ في iOS Keychain ولا تُكتب في السورس.

## GitHub Actions

- Workflow يعمل بصلاحية `contents: read` فقط.
- لا يستخدم البناء إلا Actions الرسمية المسموح بها في سياسة المستودع، مع مراجعة تحديثات إصداراتها الرئيسية.
- `scripts/check_repository_secrets.sh` يفشل البناء عند اكتشاف مفتاح خاص أو نمط
  رمز وصول معروف أو ملف اعتماد محظور.
- ملفات البناء تُرفق مع `SHA256SUMS.txt` للتحقق من السلامة.

## إعداد الأسرار لاحقاً

استخدم GitHub Environment Secrets لأي بيانات نشر مطلوبة. لا تضف قيمة سرية إلى
ملف Workflow أو ملف رأس Objective-C، ولا تشفر السورس بمفتاح محفوظ داخل المستودع
نفسه لأن ذلك لا يوفر حماية حقيقية وقد يعطل البناء.

## نطاق المشروع

يبقى فلتر التوزيع الافتراضي مقيداً بتطبيق اختبار مصرح به. لا تقبل تقارير تطلب
توسيع الحقن إلى تطبيقات جهات خارجية أو إضافة تجاوز TLS أو إخفاء جيلبريك.
